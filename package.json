{
  "name": "tariff-analyzer",
  "version": "1.0.0",
  "description": "A web application that analyzes tariff news to extract key information and provide sentiment analysis for affected sectors.",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "npm run start -w frontend -w backend",
    "dev": "npm run dev -w frontend -w backend",
    "build": "npm run build -w frontend && npm run build -w backend"
  }
}